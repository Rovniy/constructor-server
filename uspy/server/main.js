var draw = require('./draw-image');

/*setInterval(function(){
  var imageData = require('./test/image.config.json');
  draw(imageData);
},3000);*/

var imageData = require('./test/image.config.json');
draw(imageData);
